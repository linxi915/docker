### HTTP代理 - 为了方便的访问内网资源。

---

只要内网中一台电脑可以让外网访问（作为连接点），就可以让内网的资源被外网客户端所访问了，不过需要注意下安全性问题。

客户端操作其实很简单，设置HTTP代理为“连接点”的机器，然后通过域名进行访问即可。

具体配置内网资源的文件参见 nginx/reverse-proxy.conf 文件

#### 注意事项
>nginx的监听端口要与docker外部映射端口一致，不然可能会出现代理后端口丢失的问题。例如 nginx中 listen 8888端口，那么docker run 的时候需要加 -p 8888:8888